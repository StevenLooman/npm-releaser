var package_ = require('./package');


function run(vcs) {
    // pre: ask new dev version
    // work: update version, update history, diff and commit, push
}


module.exports = exports = {
    'run': run
};
